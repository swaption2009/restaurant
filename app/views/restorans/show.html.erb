<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @restoran.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @restoran.description %>
</p>

<h3>Menus</h3>
<% @menus.each do |m|%>
    <%=link_to m.title, menu_path(m)%>
<%end%>

<%= link_to "Add a Menu", new_menu_path(restoran_id: @restoran.id), class: "btn btn-default" %>
<%= link_to 'Edit', edit_restoran_path(@restoran) %> |
<%= link_to 'Back', restorans_path %>
