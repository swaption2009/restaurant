<%= form_for(restoran) do |f| %>
  <% if restoran.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restoran.errors.count, "error") %> prohibited this restoran from being saved:</h2>

      <ul>
      <% restoran.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :slug %>
    <%= f.text_field :slug %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
